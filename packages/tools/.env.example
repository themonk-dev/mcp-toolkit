# @mcp-toolkit/tools — environment fragment
# The bundled example tools (echo, health, whoami) read no env keys directly.
# Add keys here when your own tools need them, then merge them into your
# server's main .env.example.
