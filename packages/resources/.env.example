# @mcp-toolkit/resources — environment fragment
# The bundled config example reads MCP_TITLE / MCP_VERSION (defined in @mcp-toolkit/mcp/env).
# The bundled status example reads no env keys.
# Add keys here when your own resources need them.
